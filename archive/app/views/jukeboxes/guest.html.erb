<div id="ajb-guest-root">
  <div class="guest-container">
    <div class="guest-header">
      <h1 class="guest-title">🎵 Guest View</h1>
      <p class="guest-subtitle">
        <span class="status-indicator"></span>
        <%= @jukebox.name %> - Session: <%= @jukebox.session_id %>
      </p>
    </div>
    
    <div id="react-guest-app">
      <!-- React app will mount here -->
      <div class="text-center">
        <div class="spinner-border text-primary" role="status">
          <span class="visually-hidden">Loading...</span>
        </div>
        <p class="mt-3">Loading Guest Interface...</p>
      </div>
    </div>
  </div>
</div>

<script>
  // Configuration for the guest React app
  window.AJB_GUEST_CONFIG = {
    jukeboxId: '<%= @jukebox.id %>',
    jukeboxName: '<%= @jukebox.name %>',
    sessionId: '<%= @jukebox.session_id %>',
    status: '<%= @jukebox.status %>',
    hasPassword: <%= @jukebox.has_password? %>,
    password: null // Will be set by user input
  };
  
  console.log('AJB Guest Config:', window.AJB_GUEST_CONFIG);
</script>

<!-- React and ReactDOM -->
<script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
<script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>

<!-- Bootstrap for styling -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<!-- Load all guest modules -->
<script src="/javascripts/ajb/guest/models/GuestApiService.js"></script>
<script src="/javascripts/ajb/guest/models/GuestState.js"></script>
<script src="/javascripts/ajb/guest/controllers/GuestController.js"></script>
<script src="/javascripts/ajb/guest/views/GuestView.js"></script>

<!-- Load main guest app -->
<script src="/javascripts/ajb/guest/guest.js"></script>

<style>
  .guest-app {
    min-height: 100vh;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    padding: 20px;
  }
  
  .guest-container {
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .guest-header {
    text-align: center;
    color: white;
    margin-bottom: 30px;
  }
  
  .guest-title {
    font-size: 2.5rem;
    margin-bottom: 10px;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
  }
  
  .guest-subtitle {
    font-size: 1.2rem;
    opacity: 0.9;
  }
  
  .auth-container {
    background: rgba(255, 255, 255, 0.95);
    border-radius: 15px;
    padding: 40px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
  }
  
  .now-playing-view .card,
  .request-songs-view .card {
    border: none;
    border-radius: 15px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
  }
  
  .guest-header .navbar {
    border-radius: 10px;
    margin-bottom: 20px;
  }
  
  .progress-container {
    background: rgba(255, 255, 255, 0.9);
    padding: 20px;
    border-radius: 10px;
  }
  
  .list-group-item {
    border: none;
    border-bottom: 1px solid #eee;
    transition: background-color 0.2s;
  }
  
  .list-group-item:hover {
    background-color: #f8f9fa;
  }
  
  .list-group-item:last-child {
    border-bottom: none;
  }
  
  .list-group-item-warning {
    background-color: #fff3cd;
    border-color: #ffeaa7;
  }
</style>